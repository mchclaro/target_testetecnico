using System;

class Program 
{
    static void Main() 
    {
        int indice = 13, soma = 0, k = 0;

        while (k < indice)
        {
            k = k + 1;
            soma = soma + k;
        }

        console.writeline(soma);
        // valor da variavel soma vai ser 91
    }
}
